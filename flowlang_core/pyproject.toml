[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "flowlang_core"
version = "0.1.0"
description = "High-performance Rust echo propagation engine for FlowLang"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
